services:

  # Serviço da API Gateway Matrix
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: gateway_matrix_api
    restart: always
    ports:
      - "8000:8000"
    
    env_file:
      - ./.env
      
    
volumes:
  postgres_data:
